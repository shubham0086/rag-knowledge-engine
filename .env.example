# Copy to .env and fill in. .env is gitignored.
# Used by serverless mode (GeminiEmbedder) to build the static index.
GEMINI_API_KEY=your_gemini_api_key_here

# Original local mode uses Anthropic for generation (not needed for serverless build).
# ANTHROPIC_API_KEY=sk-ant-...
